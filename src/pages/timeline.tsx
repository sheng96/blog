import { Layout } from "@/components/Layout";

const def =()=> {
    return <Layout content={FC()}></Layout>;
};

const FC=()=>{
    return <main className={`w-fit mt-3`}>
        这是一个时间线
    </main>
}

export default def
